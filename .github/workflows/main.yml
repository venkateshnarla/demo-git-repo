name: ngnix-deployment
on: push
jobs:
  my-job:
    runs-on: Linux
    steps:
      - name: checkout
        uses: actions/checkout@3
      - name: update website
        run: cp -vrf . /var/www/html
      - name: check files
        run: ls -l /var/www/html     
        

